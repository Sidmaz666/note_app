// Re-export from preference hook for backward compatibility
// The preference hook provides the effective color scheme based on user preference
export { useColorScheme } from './use-color-scheme-preference';
